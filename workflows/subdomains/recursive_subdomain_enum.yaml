vars:
  OUTPUT_PATH: output/tesla
  NOTIFY: false

parallel: true
modules:

  - name: subfinder_recursive_enum
    cmds:
      - bash workflows/subdomains/src/subfinder_enum.sh '{{OUTPUT_PATH}}/subs/all.subs.txt' '{{OUTPUT_PATH}}'
  
  - name: chaos_recursive_enum
    cmds:
      - bash workflows/subdomains/src/chaos_enum.sh '{{OUTPUT_PATH}}/subs/all.subs.txt' '{{OUTPUT_PATH}}'