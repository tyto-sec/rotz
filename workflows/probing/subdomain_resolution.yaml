vars:
  OUTPUT_PATH: output/vuln
  SUBDOMAINS: output/vuln/subs/all.subs.txt
  NOTIFY: true

parallel: false
modules:
  - name: httpx_resolution
    cmds: 
      - bash workflows/probing/src/httpx_resolution.sh '{{SUBDOMAINS}}' '{{OUTPUT_PATH}}' '{{NOTIFY}}' 
  